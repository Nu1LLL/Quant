# 模拟盘状态目录

第一次运行 `paper_trader.py` 后，会在这里生成：

- `state.json`：现金、持仓、止损和最后处理时间。
- `events.csv`：模拟买入和卖出事件。
- `equity.csv`：每次运行的账户权益。

如需紧急停止，在本目录创建一个名为 `EMERGENCY_STOP` 的空文件。程序检测到该文件后不会访问行情，也不会更新模拟交易。

不要手动修改 `state.json`。策略参数变化时，程序会拒绝继续沿用旧状态。
